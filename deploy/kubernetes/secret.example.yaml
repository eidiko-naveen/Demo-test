# Example only. Do not commit a populated Secret.
# Prefer your approved secret manager, External Secrets Operator, Sealed Secrets,
# or the kubectl/oc command documented in DEVOPS_HANDOFF.md.
apiVersion: v1
kind: Secret
metadata:
  name: ocp-monitor-secrets
  namespace: ocp-monitor
type: Opaque
stringData:
  GROQ_API_KEY: REPLACE_ME
  POSTGRES_PASSWORD: REPLACE_ME
  SMTP_USER: REPLACE_ME_IF_EMAIL_IS_ENABLED
  SMTP_PASSWORD: REPLACE_ME_IF_EMAIL_IS_ENABLED
  SENDGRID_API_KEY: REPLACE_ME_IF_SENDGRID_IS_USED
  GOOGLE_CLIENT_ID: REPLACE_ME_IF_ESCALATION_IS_ENABLED
  GOOGLE_CLIENT_SECRET: REPLACE_ME_IF_ESCALATION_IS_ENABLED
  GOOGLE_REFRESH_TOKEN: REPLACE_ME_IF_ESCALATION_IS_ENABLED
