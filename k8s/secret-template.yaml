# TEMPLATE ONLY — DO NOT commit real secret values.
# Preferred: let the DevOps/secret-management process create this Secret.
apiVersion: v1
kind: Secret
metadata:
  name: eidiko-chatbot-secrets
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "<provide-through-approved-secret-management>"
  # Put the complete Google OAuth client JSON in this key.
  google-credentials.json: |
    <provide-through-approved-secret-management>
