# ============================================================
# EIDIKO Chatbot — Python Dependencies
# Python version : 3.12.3
# Generated on   : 2026-08-27
#
# Install:
#   pip install -r requirements.txt
#
# Upgrade all and re-pin:
#   pip install --upgrade -r requirements.txt
#   pip freeze > requirements.txt
# ============================================================

# ── Web Framework ────────────────────────────────────────────
flask==3.1.3                    # Web framework + routing
werkzeug==3.1.7                 # Flask dependency: WSGI utilities, secure_filename
markupsafe==3.0.3               # Flask dependency: safe HTML escaping (escape())
gunicorn==26.2.0                # Production WSGI server (replaces Flask dev server)

# ── Google APIs ──────────────────────────────────────────────
google-auth==2.57.0             # Google OAuth2 credentials & token refresh
google-auth-oauthlib==1.4.1     # OAuth flow (Flow, InstalledAppFlow)
google-api-python-client==2.199.0  # Gmail, Drive, Calendar API client

# ── Claude / Anthropic ───────────────────────────────────────
anthropic==1.1.0                # Claude API client (classify_intent, summarize)

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.2.2            # Load .env file into os.environ
requests==2.33.1                # HTTP client (used for OAuth token revocation)
