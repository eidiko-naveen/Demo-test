apiVersion: v1
kind: Service
metadata:
  name: ocp-monitor-dashboard
  namespace: ocp-monitor
  labels:
    app.kubernetes.io/name: ocp-monitor
    app.kubernetes.io/component: dashboard
spec:
  selector:
    app.kubernetes.io/name: ocp-monitor
    app.kubernetes.io/component: dashboard
  ports:
    - name: http
      port: 8501
      targetPort: http
      protocol: TCP
