# Copy to deploy/secrets.env, populate locally, and never commit that file.
GROQ_API_KEY=
POSTGRES_PASSWORD=
SMTP_USER=
SMTP_PASSWORD=
SENDGRID_API_KEY=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
# Required only before HITL_EXECUTION_ENABLED is changed to true.
HITL_APPROVAL_SECRET=
