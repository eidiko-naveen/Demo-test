[project]
name = "mnemorag-frontend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.39.0",
    "requests>=2.32.0",
    "pydantic-settings>=2.6.0",
]

[tool.uv]
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"