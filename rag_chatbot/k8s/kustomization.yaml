apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: enterprise-rag-chatbot
    newName: registry.example.com/approved/enterprise-rag-chatbot
    newTag: "0.1.0"
