[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "enterprise-rag-chatbot"
version = "0.1.0"
requires-python = ">=3.10,<3.14"

[tool.setuptools]
packages = ["agents", "auth", "config", "database", "memory", "models", "rag", "services", "ui", "utils"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
pythonpath = ["."]